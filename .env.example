# Host port that maps to the container's 8000.
WPD_HOST_PORT=8123

# Maximum number of seats per table.
WPD_MAX_SEATS=9

# Optional: force the base URL used in join QR codes.
# Leave empty to auto-detect from the request (works on a LAN).
# WPD_PUBLIC_BASE_URL=http://192.168.1.10:8123
